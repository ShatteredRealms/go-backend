agones:
  namespace: 'default'
  allocator:
    remote:
      host: 'localhost'
uptrace:
  host: "localhost"
  port: 14317
  id: "2"
  token: "project2_secret_token"
keycloak:
  baseUrl: "http://localhost:8080"
  realm: "default"
