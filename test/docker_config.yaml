characters:
  remote:
    host: sro-character
  db:
    master:
      host: postgres
  kafka:
    host: kafka
  keycloak:
    baseUrl: http://keycloak:8080
chat:
  kafka:
    host: kafka
  remote:
    host: sro-chat
  db:
    master:
      host: postgres
  keycloak:
    baseUrl: http://keycloak:8080
gamebackend:
  remote:
    host: sro-gamebackend
  db:
    master:
      host: postgres
  kafka:
    host: kafka
  keycloak:
    baseUrl: http://keycloak:8080
uptrace:
  dsn: http://project2_secret_token@uptrace:14317/2
